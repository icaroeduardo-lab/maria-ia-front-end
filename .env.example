# URL base da API do backend (Maria Chat)
VITE_API_URL=http://localhost:3000

# JWT fixo do painel (sem tela de login por decisão de jul/2026).
# Gerar via POST /auth/login com o admin seed e colar aqui.
# Atenção: expira em 8h — renovar quando o painel exibir aviso de 401.
VITE_API_TOKEN=
